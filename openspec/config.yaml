schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
context: |
  Python 3.12 Ports & Adapters app with a Reflex-native GUI.
  Do not migrate to React, Next.js, or Vite.
  Before generic search changes, read context.md for orientation only, then
  AGENTS.md, OpenSpec, repository contracts, architecture docs, and
  docs/ai-contracts/. The change multi-market-search-semantics is planned as
  staged PRs A-E. Implementation PRs A and B are currently implemented on
  main. PRs C-E remain unimplemented. Source of truth: OpenSpec > repository
  implementation/contracts > architecture docs > AI schemas/golden fixtures >
  context.md / implementation convenience.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours

# Per-operation guidance (optional)
# Add advisory guidance for how apply and archive work should be conducted.
# This is separate from artifact rules above.
# Example:
#   operations:
#     apply:
#       guidance:
#         - Keep test summaries concise
#     archive:
#       guidance:
#         - Summarize the archive outcome before finishing
