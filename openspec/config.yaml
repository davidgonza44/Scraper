schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
context: |
  Python 3.12 Ports & Adapters app with a Reflex-native GUI.
  Do not migrate to React, Next.js, or Vite.
  Before generic search changes, read AGENTS.md and docs/ai-contracts/.
  OpenSpec change multi-market-search-semantics is staged PRs A-E; do not
  implement later stages early.
  Source of truth: OpenSpec > repository contracts > architecture docs >
  AI contract schemas/fixtures > implementation convenience.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours

# Per-operation guidance (optional)
# Add advisory guidance for how apply and archive work should be conducted.
# This is separate from artifact rules above.
# Example:
#   operations:
#     apply:
#       guidance:
#         - Keep test summaries concise
#     archive:
#       guidance:
#         - Summarize the archive outcome before finishing
